The Python Notebook distanceMatrixCreation.ipynb generates an html file distances_total.html which when opened with a browser is using javascript queries to create the matrix of distances between the main cities of each french department (size 95x95). This code is using the Google maps API which is restricted to a limited number of requests. Thus the matrix is never filled totally... (cf screenshot)